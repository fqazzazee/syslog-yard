// Shim: the suite-wide theme helper lives in apps/shared/web — edit it there.
export { getTheme, applyTheme, toggleTheme, initTheme } from "../../../shared/web/src/theme";
export type { Theme } from "../../../shared/web/src/theme";
