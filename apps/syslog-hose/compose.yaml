services:
  syslog-hose:
    build: .
    image: syslog-hose:latest
    container_name: syslog-hose
    ports:
      - "8080:8080"
    volumes:
      - syslog-hose-data:/data
    restart: unless-stopped

volumes:
  syslog-hose-data:
