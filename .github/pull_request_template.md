## What this changes

<!-- One or two sentences: what and why. Link the issue if there is one. -->

## How it was tested

<!-- `go test ./...` results per touched module; for visible changes, what you
     exercised through the UI (`scripts/yardctl up` / `smoke`). -->

## Checklist

- [ ] `go test ./...` passes in every touched module
- [ ] README / `docs/` updated if behavior they describe changed
- [ ] New dependencies (if any) are justified in the PR and added to `docs/CREDITS.md`
